// Файл: shapes/GraphicalCircle.java
package shapes;

public class GraphicalCircle extends GraphicalEllipse {
    public GraphicalCircle(int x, int y, int radius) {
        super(x, y, radius, radius);
    }
}